{
  "name": "stripe",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "twilio-run",
    "deploy": "twilio-run deploy"
  },
  "devDependencies": {
    "twilio-run": "^2.6.0"
  },
  "engines": {
    "node": "10"
  },
  "dependencies": {
    "airtable": "^0.10.1",
    "monkeylearn": "^3.4.0",
    "shortid": "^2.2.16",
    "stripe": "^8.141.0"
  }
}
